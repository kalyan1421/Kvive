<?xml version="1.0" encoding="utf-8"?>
<!-- Universal keyboard layout - works within keyboard container -->
<com.kvive.keyboard.SwipeKeyboardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/keyboard_view"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:clipToPadding="false"
    android:clipToOutline="false"
    android:padding="0dp"
    android:layout_margin="0dp"
    android:background="@android:color/transparent"
    android:keyBackground="@drawable/key_background_transparent"
    android:keyPreviewLayout="@layout/keyboard_key_preview"
    android:keyPreviewOffset="0dp"
    android:keyPreviewHeight="@dimen/key_preview_height"
    android:keyTextSize="@dimen/key_text_size"
    android:labelTextSize="@dimen/key_label_text_size"
    android:popupLayout="@layout/keyboard_popup_keyboard"
    android:verticalCorrection="0dp"
    android:shadowColor="@android:color/transparent"
    android:shadowRadius="0" />
    <!-- ✅ REMOVED: fitsSystemWindows - handled by keyboard container -->
    <!-- ✅ CHANGED: layout_height to wrap_content for proper container sizing -->
